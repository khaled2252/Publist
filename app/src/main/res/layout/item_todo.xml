<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <com.google.android.material.radiobutton.MaterialRadioButton
        android:id="@+id/chipButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:buttonTint="@color/sunsetOrange"
        android:layout_marginStart="10dp"
        app:layout_constraintEnd_toStartOf="@id/wishTextView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/wishTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="Check the Landmarks of the city"
        android:textColor="@color/outerSpace"
        android:layout_marginTop="15dp"
        app:layout_constrainedWidth="true"
        app:layout_constraintStart_toEndOf="@+id/chipButton"
        app:layout_constraintEnd_toStartOf="@+id/finishedThisTextView"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/finishedThisTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="15dp"
        android:layout_marginEnd="15dp"
        android:layout_marginTop="15dp"
        android:textColor="@color/Manatee"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/wishTextView"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="+50 Finished this" />


</androidx.constraintlayout.widget.ConstraintLayout>